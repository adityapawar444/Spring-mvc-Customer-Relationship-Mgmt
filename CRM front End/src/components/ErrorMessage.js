function ErrorMessage(){

    return (
      <div class="alert alert-danger" role="alert">
        Connection Failed. Can't fetch data at the moment.
      </div>
    );
}

export default ErrorMessage;